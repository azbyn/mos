editText:
linewrap
string escape sequence highlighting
squiggly lines
cursor blink

outText:
linewrap

misc:
add good on screen keyboard
try qvector instead of std::vector
saves, loads

dlang:
replace cont with a lexer that ignores newlines & indent after certain tokens like +

instead of complicated captures use .staticMember
remove compile time name check
import x : *

@mosenum attribute

lambdas no assigns allowed
no more free vars
remove exceptions (like using opCall instead of switching)

merge memberFunction and static function

replace addStruct to an addConst

aliased captures ie. [x=somevar]():

default args ie. fun x(a, b=5):
tuple unpacking ie. (a, b) = (1, 2)
use smarter algorithm for dictionary
spacing in dlang output error
?slices
?pass
?del keyword
