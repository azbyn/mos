editText:
linewrap
string escape sequence highlighting
squiggly lines
cursor blink

outText:
linewrap

misc:
add good on screen keyboard
try qvector instead of std::vector

qml:
add () elif prop struct module

dlang:
test calling static with this
tuple unpacking ie. (a, b) = (1, 2)
variadics
use smarter algorithm for dictionary
?pass
spacing in dlang output
opCat no overloads only string concatenation (like perl5's '.')
using x : y, z (like from x import y, z)
?del keyword
